import * as React from "react"

function H2HeadingGroup (heading, subheading) {
    return (
        <>
        <h2>{heading}</h2>
        <p className="subheading">{subheading}</p>
        </>
    )
}

export default H2HeadingGroup
